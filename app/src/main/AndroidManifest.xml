<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tokyo.tkw.thinmp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".util.ActivityUtil"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="ThinMP"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AlbumActivity" />
        <activity android:name=".activity.ArtistActivity" />
        <activity android:name=".activity.PlaylistDetailActivity" />
        <activity android:name=".activity.PlayerActivity" />
        <activity android:name=".activity.FavoriteSongEditActivity" />

        <service
            android:name=".player.MusicService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".activity.FavoriteArtistEditActivity"></activity>
    </application>

</manifest>